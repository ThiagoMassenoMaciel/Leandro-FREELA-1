'use strict';

/**
 * formulario-inscricao service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::formulario-inscricao.formulario-inscricao');
