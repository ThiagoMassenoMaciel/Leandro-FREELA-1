'use strict';

/**
 * formulario-inscricao controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::formulario-inscricao.formulario-inscricao');
