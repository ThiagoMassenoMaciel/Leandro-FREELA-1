'use strict';

/**
 * formulario-inscricao router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::formulario-inscricao.formulario-inscricao');
