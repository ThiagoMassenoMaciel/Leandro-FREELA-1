'use strict';

/**
 * pagina-casos-juridico router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::pagina-casos-juridico.pagina-casos-juridico');
